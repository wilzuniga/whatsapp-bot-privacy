rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow access from Firebase Functions (admin SDK)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
